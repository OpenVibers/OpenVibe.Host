{
  "id": "host.domain.manage",
  "version": "1.0",
  "owner": "host",
  "status": "active",
  "visibility": "first-party",
  "description": "Add a custom domain to a static site (never an OpenVibe domain; not in sandbox projects), read its DNS TXT verification and routing records, trigger a verification check, and remove it. A custom domain is served only after its TXT record is verified; TLS certificates are issued by an operator and never pass through this capability.",
  "permissions": [
    "host:write"
  ],
  "resourceConstraints": [
    "project"
  ],
  "quotaClass": "host-manage",
  "events": [
    "host.domain.verified"
  ],
  "implementedBy": [
    "GET /api/v1/sites/:id/domains",
    "POST /api/v1/sites/:id/domains",
    "POST /api/v1/domains/:id/verify",
    "DELETE /api/v1/domains/:id"
  ]
}
